# datastraw

## Structure
- `/Frontend` - React/Vite web application
- `/Backend` - Node/Express proxy API

## Environment
Ensure both frontend and backend directories have their respective `.env` files configured before starting.

## .gitignore Note
This root `.gitignore` applies globally, but each sub-directory also contains specific `.gitignore` rules for the frontend and backend respectively.
